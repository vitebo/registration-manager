# `~/router/`

Essa pasta não teve atualizações, apenas tem a responsabilidade de conhecer as rotas da aplicação e
qual componente renderizar de acordo com cada rota. Inclusive acretido que renomear a rota de `/new-user`
para `/new-registration` faria bastante sentido. Usar nomes em `pt-BR` na url parece ser super bem-vindo também
ja que todo o texto da aplicação está em português.

Eu não parei para ver direito como funciona o lazy load de páginas em uma `SPA` `react` mas isso entraria nessa camada
também
